<Window x:Class="AlsuAuthEmpl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AlsuAuthEmpl"
        mc:Ignorable="d"
        FontSize="24"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Роль в системе" />
            <ComboBox x:Name="cbRole">
                <ComboBoxItem Content="Админ" />
                <ComboBoxItem Content="Пользователь" />
                <ComboBoxItem Content="Левый чел" />
            </ComboBox>
            <TextBlock Text="Login"/>
            <TextBox x:Name="tbLogin" />
            <TextBlock Text="Pass" />
            <TextBox x:Name="tbPass" />
            <Button Content="auth" Click="Button_Click" />
            <Button Content="ВХОД ЧЕРЕЗ КОМБОБОКС С ВЫБОРОМ РОЛИ" Click="roleClick"/>
        </StackPanel>
    </Grid>
</Window>
